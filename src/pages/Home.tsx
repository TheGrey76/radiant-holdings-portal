import HeroNetwork from '@/components/HeroNetwork';

const Home = () => {
  return (
    <div className="min-h-screen">
      {/* New Network Hero Section */}
      <HeroNetwork />
    </div>
  );
};

export default Home;